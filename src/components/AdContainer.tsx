import React from 'react';

/**
 * This component just shows an ad below the mistyped words / slow words sections
 */
const AdContainer = ({ word, typedWord, wordIndex, currentWord }: { word: string[], typedWord: string[], wordIndex: number, currentWord: number }): React.ReactElement => {
    
    return (
        <>
        </>
    );
}

export default AdContainer;