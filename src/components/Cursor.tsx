import React from 'react';

/**
 * pipe cursor component 
 */
const Cursor = ({ word, typedWord }: { word: string[], typedWord: string[] }): React.ReactElement => {
    return (
        <>
        </>
    );
}

export default Cursor;